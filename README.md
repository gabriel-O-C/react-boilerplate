# Boilerplate for ReactJS projects

### Configs used:
1. editorconfig
2. Eslint
3. Storybook
4. commitLint

### Setup project:
---
To run the project you should follow these steps:
1. Run `yarn` or `npm install`
2. Run `yarn husky install` or `npx husky install`
